import React from 'react';
import ReactDOM from 'react-dom/client';
import AssetPortfolioApp from './AssetPortfolioApp';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <AssetPortfolioApp />
  </React.StrictMode>
);

